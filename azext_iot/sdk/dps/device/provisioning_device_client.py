# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from msrest.service_client import SDKClient
from msrest import Serializer, Deserializer
from msrestazure import AzureConfiguration
from .version import VERSION
from .operations.runtime_registration_operations import RuntimeRegistrationOperations
from . import models
from ....constants import USER_AGENT


class ProvisioningDeviceClientConfiguration(AzureConfiguration):
    """Configuration for ProvisioningDeviceClient
    Note that all parameters used to create this instance are saved as instance
    attributes.

    :param credentials: Credentials needed for the client to connect to Azure.
    :type credentials: :mod:`A msrestazure Credentials
     object<msrestazure.azure_active_directory>`
    :param str base_url: Service URL
    """

    def __init__(
            self, credentials, base_url=None):

        if credentials is None:
            raise ValueError("Parameter 'credentials' must not be None.")
        if not base_url:
            base_url = 'https://global.azure-devices-provisioning.net'

        super(ProvisioningDeviceClientConfiguration, self).__init__(base_url)

        self.add_user_agent('provisioningdeviceclient/{}'.format(VERSION))
        self.add_user_agent(USER_AGENT) # @c-ryan-k - added CLI UA string

        self.credentials = credentials


class ProvisioningDeviceClient(SDKClient):
    """API for device runtime operations with the Azure IoT Hub Device Provisioning Service

    :ivar config: Configuration for client.
    :vartype config: ProvisioningDeviceClientConfiguration

    :ivar runtime_registration: RuntimeRegistration operations
    :vartype runtime_registration: dps.operations.RuntimeRegistrationOperations

    :param credentials: Credentials needed for the client to connect to Azure.
    :type credentials: :mod:`A msrestazure Credentials
     object<msrestazure.azure_active_directory>`
    :param str base_url: Service URL
    """

    def __init__(
            self, credentials, base_url=None):

        self.config = ProvisioningDeviceClientConfiguration(credentials, base_url)
        super(ProvisioningDeviceClient, self).__init__(self.config.credentials, self.config)

        client_models = {k: v for k, v in models.__dict__.items() if isinstance(v, type)}
        self.api_version = '2019-03-31'
        self._serialize = Serializer(client_models)
        self._deserialize = Deserializer(client_models)

        self.runtime_registration = RuntimeRegistrationOperations(
            self._client, self.config, self._serialize, self._deserialize)
