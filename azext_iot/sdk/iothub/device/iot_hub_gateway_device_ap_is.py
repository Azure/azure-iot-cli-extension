# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for
# license information.
#
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from msrest.service_client import SDKClient
from msrest import Serializer, Deserializer
from msrestazure import AzureConfiguration
from .version import VERSION
from .operations.device_operations import DeviceOperations
from . import models
from azext_iot.constants import USER_AGENT


class IotHubGatewayDeviceAPIsConfiguration(AzureConfiguration):
    """Configuration for IotHubGatewayDeviceAPIs
    Note that all parameters used to create this instance are saved as instance
    attributes.

    :param credentials: Credentials needed for the client to connect to Azure.
    :type credentials: :mod:`A msrestazure Credentials
     object<msrestazure.azure_active_directory>`
    :param str base_url: Service URL
    """

    def __init__(
            self, credentials, base_url=None):

        if credentials is None:
            raise ValueError("Parameter 'credentials' must not be None.")
        if not base_url:
            base_url = 'https://fully-qualified-iothubname.azure-devices.net'

        super(IotHubGatewayDeviceAPIsConfiguration, self).__init__(base_url)

        self.add_user_agent('iothubgatewaydeviceapis/{}'.format(VERSION))
        self.add_user_agent(USER_AGENT)  # @digimaun

        self.credentials = credentials


class IotHubGatewayDeviceAPIs(SDKClient):
    """IotHubGatewayDeviceAPIs

    :ivar config: Configuration for client.
    :vartype config: IotHubGatewayDeviceAPIsConfiguration

    :ivar device: Device operations
    :vartype device: device.operations.DeviceOperations

    :param credentials: Credentials needed for the client to connect to Azure.
    :type credentials: :mod:`A msrestazure Credentials
     object<msrestazure.azure_active_directory>`
    :param str base_url: Service URL
    """

    def __init__(
            self, credentials, base_url=None):

        self.config = IotHubGatewayDeviceAPIsConfiguration(credentials, base_url)
        super(IotHubGatewayDeviceAPIs, self).__init__(self.config.credentials, self.config)

        client_models = {k: v for k, v in models.__dict__.items() if isinstance(v, type)}
        self.api_version = '2019-10-01'
        self._serialize = Serializer(client_models)
        self._deserialize = Deserializer(client_models)

        self.device = DeviceOperations(
            self._client, self.config, self._serialize, self._deserialize)
