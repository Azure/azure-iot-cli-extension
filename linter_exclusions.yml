dt endpoint create:
  rule_exclusions:
    - require_wait_command_if_no_wait
dt endpoint create servicebus:
  parameters:
    servicebus_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
dt endpoint create eventgrid:
  parameters:
    eventgrid_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
dt endpoint create eventhub:
  parameters:
    eventhub_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
dt data-history connection create:
  rule_exclusions:
    - require_wait_command_if_no_wait
dt data-history connection create adx:
  parameters:
    adx_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
    eh_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot dps enrollment update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot dps enrollment-group update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub configuration update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub device-identity update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub device-twin update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub distributed-tracing update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub module-identity update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub module-twin update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot hub state migrate:
  parameters:
    orig_resource_group_name:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
    resource_group_name:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint create cosmosdb-container:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint create eventhub:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint create servicebus-queue:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint create servicebus-topic:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint create storage-container:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint update cosmosdb-container:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint update eventhub:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint update servicebus-queue:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint update servicebus-topic:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub message-endpoint update storage-container:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot edge deployment update:
  parameters:
    auth_type_dataplane:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central export destination update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central export update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central device update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central device-template update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central device-group update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central file-upload-config update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central organization update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central user update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central device attestation update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central enrollment-group update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    enabled:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central scheduled-job update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    batch_type:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    threshold_type:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot central device twin update:
  parameters:
    api_version:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
    central_dns_suffix:
      rule_exclusions:
        - no_parameter_defaults_for_update_commands
iot du update:
  rule_exclusions:
    - require_wait_command_if_no_wait

# DPS command parameter naming exclusions
iot dps linked-hub create:
  parameters:
    apply_allocation_policy:
      rule_exclusions:
        - option_length_too_long
    hub_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot dps linked-hub update:
  parameters:
    apply_allocation_policy:
      rule_exclusions:
        - option_length_too_long
iot hub certificate list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub consumer-group list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub message-enrichment list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub policy list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub route list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub routing-endpoint create:
  parameters:
    endpoint_resource_group:
      rule_exclusions:
        - parameter_should_not_end_in_resource_group
iot hub routing-endpoint list:
  rule_exclusions:
    - no_ids_for_list_commands

# Wait command exclusions for commands with --no-wait
iot dps linked-hub:
  rule_exclusions:
    - require_wait_command_if_no_wait
iot dps policy:
  rule_exclusions:
    - require_wait_command_if_no_wait
iot hub policy:
  rule_exclusions:
    - require_wait_command_if_no_wait
iot hub:
  rule_exclusions:
    - require_wait_command_if_no_wait

# These asynchronous POST actions do not expose a retrievable provisioning-state resource.
iot adr ns registry-device auth-profile:
  rule_exclusions:
    - require_wait_command_if_no_wait
iot adr ns report:
  rule_exclusions:
    - require_wait_command_if_no_wait

# An extended location is a required custom-location scope, not an Azure region.
iot adr ns asset create:
  parameters:
    extended_location:
      rule_exclusions:
        - no_required_location_param
iot adr ns discovered-asset create:
  parameters:
    extended_location:
      rule_exclusions:
        - no_required_location_param
iot adr ns discovered-device create:
  parameters:
    extended_location:
      rule_exclusions:
        - no_required_location_param

# Additional list commands that legitimately use --ids parameter
iot dps enrollment list:
  rule_exclusions:
    - no_ids_for_list_commands
iot dps enrollment-group list:
  rule_exclusions:
    - no_ids_for_list_commands
iot dps enrollment-group registration list:
  rule_exclusions:
    - no_ids_for_list_commands
iot dps registration list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub message-endpoint list:
  rule_exclusions:
    - no_ids_for_list_commands
iot hub message-route list:
  rule_exclusions:
    - no_ids_for_list_commands
